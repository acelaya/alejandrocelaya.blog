---
layout: default
title: Tags
use:
    - posts_tags
---
<h2>Tags</h2>

<div class="tags-list">
    {% for tag,posts in data.posts_tags %}
        <a class="btn btn-default" href="{{ site.url }}/tag/{{ tag | url_encode(true) }}/">{{ tag }}</a>
    {% endfor %}
</div>
