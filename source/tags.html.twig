---
layout: default
title: Tags
use:
    - posts_tags
---
<h2>Tags</h2>

<div>
    {% for tag,posts in data.posts_tags %}
        <a href="{{ site.url }}/tag/{{ tag | url_encode(true) }}">{{ tag }}</a>
    {% endfor %}
</div>
